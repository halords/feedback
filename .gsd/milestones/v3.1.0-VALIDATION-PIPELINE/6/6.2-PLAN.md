---
phase: 6
plan: 2
wave: 2
---

# Plan 6.2: API Authentication Policy & Caching

## Objective
Address medium/low priority audit findings by securing the 'Offices' endpoint against unauthenticated public access, implementing memory caching for assignee lookups during bulk generation, and managing SWR caches upon logout.

## Context
- .gsd/ROADMAP.md (Phase 6)
- c:\Users\ADMIN-LPT-022\.gemini\antigravity\brain\8f1f6be8-a2eb-40f9-a2d5-faa635b75e32\system_audit_report.json
- src/app/api/offices/route.ts
- src/lib/services/officeService.ts (or wherever `getAllOfficeAssignees` is defined)
- src/components/layout/Shell.tsx (or Context where logout triggers)

## Tasks

<task type="auto">
  <name>Enforce Authentication on GET /api/offices</name>
  <files>src/app/api/offices/route.ts</files>
  <action>
    - Ensure the `GET` endpoint verifies the session using `verifySession()` rather than silently querying.
    - If no valid session is found, respond immediately with `401 Unauthorized`.
    - **Note:** Standard users should still be able to hit this endpoints to populate dropdowns, so it must not be restricted strictly to Superadmins, only to authenticated users (and the logic should still filter offices returned if applicable).
  </action>
  <verify>curl -I http://localhost:3000/api/offices (Should return 401 Unauthorized for unauthenticated requests)</verify>
  <done>Fetching the office list requires a valid JWT session.</done>
</task>

<task type="auto">
  <name>Implement Caching for Office Assignees</name>
  <files>src/lib/services/officeService.ts</files>
  <action>
    - Locate the `getAllOfficeAssignees()` function.
    - Implement a simple memory cache (e.g., using a module-level variable to store the result and a timestamp `lastFetched`).
    - Cache the result for 5 minutes (`300000ms`).
    - If the cache is valid, return the memory map instead of performing a fresh Firestore query.
  </action>
  <verify>Observe console logs (add a debug log "Fetching Assignees from Firestore" vs "Cache Hit") during bulk report generation.</verify>
  <done>The number of Firestore reads drops when repeatedly querying the assignee map within 5 minutes.</done>
</task>

<task type="auto">
  <name>Manage SWR Cache on Logout</name>
  <files>src/context/AuthContext.tsx, src/components/layout/Shell.tsx</files>
  <action>
    - Import `mutate` from `swr` in the component/context handling the logout action.
    - When `signOut()` or the logout handler fires, invoke `mutate(
      () => true, // Which keys to clear (all)
      undefined, // undefined explicitly
      { revalidate: false }
    )` to completely flush the local SWR cache.
    - Alternative SWR cache clearing methods specific to SWR v2+ (`cache.clear()`) can be used if `useSWRConfig()` is implemented.
  </action>
  <verify>Login -> view analytics -> logout -> intercept network requests or log SWR state. Cannot test headless easily, requires manual run verification.</verify>
  <done>SWR caches do not bleed stale data across different user sessions on the same client.</done>
</task>

## Success Criteria
- [ ] `GET /api/offices` correctly rejects anonymous requests.
- [ ] `getAllOfficeAssignees` hits cache during standard use, drastically cutting redundant database reads.
- [ ] `logout` wipes client-side SWR memory.
