---
phase: 1
plan: 2
wave: 1
---

# Plan 1.2: Security Hardening & Data Readiness

## Objective
Implement strict input validation on APIs to prevent malformed data entry and prepare the database for better query performance by creating a migration strategy for legacy date fields.

## Context
- src/app/api/responses/route.ts
- src/lib/services/responseService.ts
- src/lib/services/physicalReportService.ts

## Tasks

<task type="auto">
  <name>Implement Zod API Validation</name>
  <files>
    - src/lib/validation/apiSchemas.ts
    - src/app/api/responses/route.ts
  </files>
  <action>
    - Install `zod` if not already present (check package.json).
    - Create `src/lib/validation/apiSchemas.ts` with schemas for Response and Dashboard API inputs.
    - Integrate validation into `src/app/api/responses/route.ts` as a pilot.
  </action>
  <verify>Check for the existence of schemas and their usage in the responses API route.</verify>
  <done>
    `responses` API route rejects invalid JSON bodies with a 400 error.
  </done>
</task>

<task type="auto">
  <name>Create Date Migration Script</name>
  <files>
    - scripts/migrate-dates.js
  </files>
  <action>
    - Create a script that iterates through a small sample of `Responses` and copies `data.Date` into a new `date_iso` field (format `YYYY-MM-DD`).
    - The script should be idempotent and provide dry-run capabilities.
    - This prepares the way for native Firestore range queries.
  </action>
  <verify>Review the script for safety features (limit, dry-run, error handling).</verify>
  <done>
    A functional, safe migration script exists in the `scripts/` directory.
  </done>
</task>

## Success Criteria
- [ ] Zod validation is active on at least one critical API route.
- [ ] A verified migration path for date fields is ready for execution.
