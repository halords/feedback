---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Database Migration Execution

## Objective
Execute the system-wide migration to add sortable date fields (`date_iso` and `period_iso`) to all documents in the `Responses` and `physical_report` collections.

## Context
- scripts/migrate-system.js
- .gsd/ROADMAP.md

## Tasks

<task type="auto">
  <name>Execute System Migration</name>
  <files>
    - scripts/migrate-system.js
  </files>
  <action>
    Run the migration script using `node scripts/migrate-system.js`.
    Ensure that the output confirms successful updates for both collections.
    - WHAT TO WATCH FOR: Ensure no "Invalid date" warnings for large blocks of data.
  </action>
  <verify>Check console output for "TOTAL SYSTEM MIGRATION COMPLETE".</verify>
  <done>
    `Responses` documents have `date_iso` field.
    `physical_report` documents have `period_iso` field.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Verify Data in Firestore Console</name>
  <files>
    - Responses collection
    - physical_report collection
  </files>
  <action>
    Manually check 3-5 random documents in both collections via the Firebase Console to ensure the new fields exist and contain the expected ISO strings.
  </action>
  <verify>Manually confirmed by user.</verify>
  <done>
    User confirms fields are present.
  </done>
</task>

## Success Criteria
- [ ] Database is 100% migrated with sortable date fields.
