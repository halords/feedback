---
phase: 3
plan: 3
wave: 2
---

# Plan 3.3: Optimization Toggle & Verification

## Objective
Provide a way to explicitly bypass the optimization for testing/debugging and verify that data parity is maintained between Live and Archived states.

## Context
- .gsd/SPEC.md
- src/components/layout/Shell.tsx

## Tasks

<task type="auto">
  <name>Implement Optimization Toggle</name>
  <files>
    src/context/SettingsContext.tsx
    src/components/layout/Shell.tsx
  </files>
  <action>
    Add a global toggle for "Read Optimization":
    1. Create/Update a context to store `isOptimizationEnabled` (defaulting to true).
    2. Add a toggle in the Sidebar or Settings (only for Superadmins/Developers).
    3. Pass this flag to the services (or read via header/param if needed).
    - Note: Services might need to check a cookie or header to determine if they should skip the archive check.
  </action>
  <verify>Toggle optimization OFF and confirm Firestore reads increase (via logs). Toggle ON and confirm JSON fetch.</verify>
  <done>User can switch between Live and Optimized views for verification.</done>
</task>

<task type="auto">
  <name>Final Phase Audit</name>
  <files>.gsd/phases/3/VERIFICATION.md</files>
  <action>
    Perform a consistency check:
    1. Pick a month.
    2. Archive it.
    3. Compare "Live" dashboard metrics with "Optimized" dashboard metrics.
    4. Document results in VERIFICATION.md.
  </action>
  <verify>Data matches 1:1.</verify>
  <done>System parity is empirically proven.</done>
</task>

## Success Criteria
- [ ] Optimization can be toggled via UI.
- [ ] Data consistency is 100% verified.
