---
phase: 3
plan: 1
wave: 1
---

# Plan 3.1: Metrics Dual-Tier Implementation

## Objective
Update `metricsService.ts` to implement the dual-tier fetching strategy for dashboard and report summaries.

## Context
- .gsd/phases/3/RESEARCH.md
- src/lib/services/metricsService.ts
- src/lib/firebase/admin.ts

## Tasks

<task type="auto">
  <name>Implement Archive Fetch Helper</name>
  <files>src/lib/services/storageService.ts</files>
  <action>
    Create a new utility service to handle JSON archive downloads:
    - Export an `async function getJsonArchive(path: string)` helper.
    - Use `storage.bucket(process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET)` correctly.
    - Return `null` if file doesn't exist or on error.
  </action>
  <verify>Check file exists and exports the helper.</verify>
  <done>Reusable helper is available for other services.</done>
</task>

<task type="auto">
  <name>Update getDashboardMetrics Logic</name>
  <files>src/lib/services/metricsService.ts</files>
  <action>
    Modify `getDashboardMetrics` to check for archives:
    1. Determine requested months (currently supports single or array).
    2. For each month/year:
       - Attempt to fetch `archives/{year}/{month}/metrics.json`.
       - If found: Filter the pre-calculated metrics for the requested offices.
       - If not found for any requested month: Fall back to existing Firestore aggregation for that period.
    3. Merge Results: Combine archived data with live-calculated data if a multi-month range spans both types.
  </action>
  <verify>Call getDashboardMetrics for an archived month and verify non-Firestore access (mock or console log check).</verify>
  <done>Dashboard data is served from JSON when available.</done>
</task>

## Success Criteria
- [ ] Dashboards load correctly for both archived and non-archived months.
- [ ] Archival data significantly reduces Firestore read count (observed via log or billing console).
