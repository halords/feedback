---
phase: 3
plan: 2
wave: 1
---

# Plan 3.2: Responses Dual-Tier Implementation

## Objective
Update `responseService.ts` to prioritize JSON archives for individual response tables and PDF creation.

## Context
- .gsd/phases/3/RESEARCH.md
- src/lib/services/responseService.ts
- src/lib/services/storageService.ts (created in 3.1)

## Tasks

<task type="auto">
  <name>Update getResponses Logic</name>
  <files>src/lib/services/responseService.ts</files>
  <action>
    Modify `getResponses` to integrate archives:
    1. Construct archive path: `archives/{year}/{month}/responses.json`.
    2. Attempt to fetch using `getJsonArchive`.
    3. If archived data exists:
       - Filter the array for requested offices using `offices.includes(data.Office)` (or handle 'ALL').
       - Format the records to match the `ResponseEntry` interface.
    4. If no archive: Fall back to existing chunked/global Firestore fetch.
  </action>
  <verify>Access a Responses table for an archived month and confirm data matches expected schema.</verify>
  <done>Response tables and PDF snapshots use JSON archives when available.</done>
</task>

## Success Criteria
- [ ] Response tables load data for archived months.
- [ ] Individual response details and PDF generation work seamlessly with archived data.
