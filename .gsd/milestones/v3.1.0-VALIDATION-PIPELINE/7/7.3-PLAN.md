---
phase: 7
plan: 3
wave: 2
---

# Plan 7.3: Frontend UI Behavior Update (Revised)

## Objective
Ensure the UI correctly implements the nuanced access rules:
1. Reports: `Summary/Graphs` use global scope, `Data View` uses assigned scope.
2. Responses: Global view hides classification column if non-superadmin.

## Context
- .gsd/FINALrev.md
- src/app/analytics/AnalyticsClient.tsx
- src/components/analytics/DataView.tsx
- src/app/responses/ResponsesClient.tsx
- src/components/responses/ResponsesTable.tsx (or similar)

## Tasks

<task type="auto">
  <name>Configure Scoped Data View in Reports</name>
  <files>
    src/app/analytics/AnalyticsClient.tsx
    src/components/analytics/DataView.tsx
  </files>
  <action>
    Modify the Reports page behavior:
    1. In `AnalyticsClient.tsx`, determine if the user is a non-superadmin with `analyticsEnabled`.
    2. If true, ensure that when the `Data View` tab is active, the office selection is **forced** to their assigned offices (ignoring the global "ALL" selection used for Summary/Graphs).
    3. You may need to pass a `forceScope` or similar prop to `DataView` or handle it in the `AnalyticsProvider`.
  </action>
  <verify>Ensure Data View only shows assigned offices for non-superadmins even if Summary shows global</verify>
  <done>Data View tab is strictly scoped to assigned offices for analytics users</done>
</task>

<task type="auto">
  <name>Hide Classification in Global Responses View</name>
  <files>
    src/app/responses/ResponsesClient.tsx
    src/components/responses/ResponsesTable.tsx
  </files>
  <action>
    Update the Responses UI:
    1. If the current view is "ALL" AND the user is NOT a Superadmin.
    2. Hide the "Classification" column in the responses table entirely.
    3. Ensure that if they switch to a specific office view (their own), the column reappears (if they have classification permissions).
  </action>
  <verify>Check Responses UI in global view for non-superadmin</verify>
  <done>Classification column is hidden in global view for non-superadmins</done>
</task>

## Success Criteria
- [ ] Reports page successfully mixes global Summary/Graphs with scoped Data View
- [ ] Responses page hides classification column when viewing "All Responses" if not superadmin
- [ ] No regression for Superadmins
