---
phase: 5
plan: 1
wave: 1
---

# Plan 5.1: Core Logic Implementation

## Objective
Implement a context-aware office filtering system that respects active status, data presence, and special 2025 exclusions.

## Context
- .gsd/SPEC.md
- .gsd/REVISION.md
- src/lib/services/officeService.ts
- src/app/api/offices/route.ts

## Tasks

<task type="auto">
  <name>Implement getEffectiveOfficesForPeriod</name>
  <files>src/lib/services/officeService.ts</files>
  <action>
    Export a new function `getEffectiveOfficesForPeriod(month: string, year: string)`:
    - Fetch all offices including disabled.
    - Filter out PYESDO and PCDO if year is 2025.
    - For disabled offices, check if any documents exist in 'Responses' (where Office == name and Date matches year/month) or 'physical_report' (where DEPARTMENT == name and FOR_THE_MONTH_OF matches month year).
    - Return the filtered list of offices.
  </action>
  <verify>Check the file export for the new function.</verify>
  <done>Function exists and implements the logic from REVISION.md.</done>
</task>

<task type="auto">
  <name>Update Office API Route</name>
  <files>src/app/api/offices/route.ts</files>
  <action>
    Update the GET handler to:
    - Extract `month` and `year` from searchParams.
    - If both are present, use `getEffectiveOfficesForPeriod`.
    - Otherwise, default to the existing `getAllOffices` behavior.
  </action>
  <verify>Call /api/offices?month=January&year=2025 and check if PYESDO is excluded.</verify>
  <done>API returns context-aware filtered offices.</done>
</task>

## Success Criteria
- [ ] Special 2025 exclusions are handled in the service layer.
- [ ] API provides filtered office lists for the UI.
