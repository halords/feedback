---
phase: 5
plan: 2
wave: 1
---

# Plan 5.2: Archiving Logic & Verification

## Objective
Update the archival process to respect the new inclusion rules and verify the final output.

## Context
- .gsd/SPEC.md
- .gsd/REVISION.md
- src/app/api/admin/archive/route.ts
- .gsd/phases/5/RESEARCH.md

## Tasks

<task type="auto">
  <name>Update Archival POST handler</name>
  <files>src/app/api/admin/archive/route.ts</files>
  <action>
    Modify the POST handler to:
    - Use `getEffectiveOfficesForPeriod(month, year)` to get the list of offices to archive.
    - Ensure PYESDO/PCDO are excluded from the `responses` array before saving to `responses.json` if year is 2025.
    - This ensures metrics.json (derived from office list) and responses.json (derived from Firestore snapshot) are consistent.
  </action>
  <verify>Run an archive for January 2025 and check the logs for office counts.</verify>
  <done>Archival process perfectly respects the Unified Logic Flow.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Verify UI Consistency</name>
  <files>src/app/api/offices/route.ts</files>
  <action>
    - Refresh the Reports page for 2025.
    - Verify that PYESDO and PCDO are NOT in the office dropdown.
    - Select a month with known inactive data and verify the inactive office appears.
  </action>
  <verify>Visual check in the browser.</verify>
  <done>UI reflects the revision logic correctly.</done>
</task>

## Success Criteria
- [ ] Archived JSON files contain the correct subset of data.
- [ ] Reports UI filters are context-aware and respect the revision rules.
