# Summary 2.3: User Management Update

## Completed Tasks
- [x] **Updated Add User Sync**: Refactored userService.ts/ddUser to create accounts in both Firestore and Firebase Auth using the ${idno}@feedback.internal identifier.
- [x] **UID Preservation**: Set the Firebase uid to match the idno during user creation.
- [x] **Password Change Sync**: Refactored the change-password API to simultaneously update Firebase Auth and the legacy Firestore password hash.

## Verification
- Superadmins can now add users who are automatically provisioned in Firebase Authentication.
- Password maintenance is synced across both Auth and Firestore.
