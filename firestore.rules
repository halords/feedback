rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 🔒 Lockdown: Admin SDKs (Next.js & Apps Script) bypass this.
      // Direct client-side access is forbidden.
      allow read, write: if false;
    }
  }
}
